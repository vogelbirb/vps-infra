services:
  uptime-kuma:
    image: louislam/uptime-kuma:2
    restart: unless-stopped
    networks:
      - web-net
    volumes:
      - uptime-kuma-data:/app/data
    # ports:
      # <Host Port>:<Container Port>
      # - "3001:3001"

networks:
  web-net:
    external: true

volumes:
  uptime-kuma-data:
